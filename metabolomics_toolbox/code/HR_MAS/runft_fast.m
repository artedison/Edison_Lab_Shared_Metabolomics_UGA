function runft_fast(studyInfo,template)

    %% Get the ft template
        fileread(template);
        
    %% Extract the commands
        
    
    %% Parameterize
    
    %% Adjust filepaths (relative)
   
    %% Run the file
    
    
end